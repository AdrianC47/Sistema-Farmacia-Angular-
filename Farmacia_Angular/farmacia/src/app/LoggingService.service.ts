export class LoggingService{
    enviarMensajeAConsola(mensaje:string){ //definimos un metodo para poder imprimir informacion que recibamos 
        console.log(mensaje); //desde otros lados desde otras clases
    }
}